# Staging Release Checklist

## Before Deploy

- [ ] Terraform remote state is configured in private encrypted R2-compatible
      storage; no local state or backend credentials are present in Git.
- [ ] The saved Terraform plan was reviewed for the approved server type,
      location, cost, hostnames, operator CIDRs, firewall rules, and exactly
      zero deletes or replacements.
- [ ] Terraform apply completed only after explicit infrastructure approval,
      and `cloud-init status --wait` plus
      `/var/lib/eliza-hub/bootstrap-complete` verified host bootstrap.
- [ ] Private `.env` has approved image tags and secrets.
- [ ] `services/merge-steward/src/cli.js domain-evidence` generated retained
      domain/TLS probe evidence, and assembled `domain.probeEvidence` records
      its local source path, SHA-256, timestamp, ready status, and check count.
- [ ] `scripts/image-provenance-evidence.mjs` generated image evidence with
      `@sha256:` references plus Merge Steward CI build, signature, SBOM, and
      vulnerability scan proof, with retained local audit artifact SHA-256
      provenance.
- [ ] `scripts/secret-management-evidence.mjs` generated the private secrets
      evidence block after external secret-store and rotation-policy
      attestations were recorded, with retained local audit artifact SHA-256
      provenance.
- [ ] `scripts/mail-evidence.mjs` generated the private mail evidence block
      after SMTP, invite, password reset, and notification smoke tests passed,
      with retained local audit artifact SHA-256 provenance.
- [ ] `scripts/storage-evidence.mjs` generated the private storage evidence
      block after sizing, package retention, LFS, artifact, and log retention
      reviews were recorded, with retained local audit artifact SHA-256
      provenance.
- [ ] `scripts/observability-evidence.mjs` generated the private observability
      evidence block after scrape, alert routing, log collection, retention,
      and page-alert reviews passed, with retained local audit artifact
      SHA-256 provenance.
- [ ] `scripts/sso-evidence.mjs` generated the private SSO evidence block after
      Eliza Cloud OIDC, human/agent/service identity smoke, registration lock,
      issuer restriction, recovery-admin checks, and timestamped smoke artifact
      plus read-only identity bootstrap receipt provenance passed.
- [ ] `scripts/backup-evidence.mjs` generated the private backup evidence
      block after the backup bundle, age-encrypted off-site upload, streamed
      download verification, independent recovery check, schedule, and
      retained receipt/audit SHA-256 provenance were verified.
- [ ] `scripts/database-evidence.mjs` generated the private database evidence
      block after Postgres config, migrations, checksum drift, and empty-host
      restore drill checks passed, with retained local audit artifact and
      migration/restore log SHA-256 provenance.
- [ ] `scripts/repository-evidence.mjs --require-live` generated the private
      repository protection evidence block from the steward live audit after
      Actions policy and admin-bypass reviews passed, with retained local audit
      artifact SHA-256 provenance.
- [ ] `scripts/pilot-bootstrap.mjs --apply` generated the private GitHub
      migration rehearsal receipt after the pull mirror, steward webhook,
      default branch, branch protection, repo policy, trusted agent identities,
      and pilot product surfaces were created or verified.
- [ ] `scripts/steward-evidence.mjs` generated the private steward evidence
      block with runtime provenance after production preflight, deployment
      doctor, label mirroring, bot-token permission review, and strict
      work-reservation enforcement plus strict Work-item enforcement passed.
- [ ] `scripts/merge-queue-rollout-drill.sh` passed against the deployed
      steward before live merge execution was enabled and wrote the private
      `MERGE_QUEUE_ROLLOUT_EVIDENCE_OUT` JSON artifact.
- [ ] `scripts/merge-queue-rollout-evidence.mjs` generated the private merge
      queue rollout evidence block from the safe drill JSON and staged live
      drill JSON generated by `scripts/merge-queue-live-drill-evidence.mjs`
      after worker lease, strict work-reservation, strict Work-item, branch
      namespace, verified agent run receipt, rollback drill, and human approval
      checks passed.
- [ ] `scripts/security-review-evidence.mjs` generated the private security
      review evidence block after auth, token, runner execution, and repository
      permission reviews were approved, with retained local audit artifact
      SHA-256 provenance.
- [ ] For production cutover, the applied deploy and retained post-deploy
      receipt will be assembled into the private evidence file after the deploy
      step and before routing real users or enabling live merge execution.
- [ ] `scripts/production-evidence-inventory.mjs --strict` passed against the
      private artifact root and recorded size, SHA-256, and modification time
      for every present fragment before the final evidence file was assembled.
- [ ] `scripts/production-evidence-assemble.mjs` assembled the private
      production evidence file from generated fragments outside Git.
- [ ] `scripts/host-preflight.sh` passed on the target host.
- [ ] `scripts/validate-env.sh` passes.
- [ ] `scripts/release-gate.sh` passes.
- [ ] Previous image tags are recorded.
- [ ] Approved `MERGE_STEWARD_IMAGE` is available through the private registry
      or direct `docker save` / `docker load`, and `docker image inspect
      "$MERGE_STEWARD_IMAGE"` passes on the target host.
- [ ] `scripts/backup.sh` completed.
- [ ] `scripts/restore-check.sh <backup>` passed.
- [ ] `scripts/backup-offsite.sh --backup-dir <backup> --apply` produced a
      verified upload receipt without retaining a plaintext aggregate archive.
- [ ] The off-site backup prefix has a reviewed server-side bucket lock and
      retention window, verified independently of the upload credentials.
- [ ] `scripts/restore-offsite-check.sh --receipt-remote <remote>
      --expected-receipt-sha256 <sha256> --apply` passed in a separate recovery
      environment using a separately transferred receipt digest and produced a
      linked receipt.
- [ ] `eliza-hub-backup.timer` is enabled and its next run is visible in
      `systemctl list-timers`.
- [ ] `scripts/restore-drill.sh <backup>` passed against a disposable Postgres container.
- [ ] Recovery admin, Eliza Cloud OIDC source, and steward token owner are verified.
- [ ] Reverse proxy and TLS config are unchanged or reviewed.
- [ ] Worker remains disabled unless this is an approved merge-queue drill.
- [ ] For production cutover, the final
      `RELEASE_GATE_MODE=production PRODUCTION_EVIDENCE_FILE=... scripts/release-gate.sh`
      command is planned as a post-deploy, pre-traffic/pre-live-merge gate once
      deploy and post-deploy receipts exist.

## Deploy

- [ ] First-boot or rolling-release path selected explicitly.
- [ ] `scripts/deploy.sh --mode first-boot|rolling` dry run passed and wrote
      `$ELIZA_ARTIFACT_ROOT/eliza-hub-deploy-evidence.json`.
- [ ] Images pulled from the private registry or loaded onto the host successfully.
- [ ] `scripts/deploy.sh --mode first-boot|rolling --apply` completed the
      selected path.
- [ ] On first boot, `postgres` and `forgejo` were started and healthy before
      `merge-steward-migrate` ran.
- [ ] On rolling release, existing `postgres` and `forgejo` health was verified
      before `merge-steward-migrate --no-deps` ran.
- [ ] `merge-steward-migrate` completed successfully.
- [ ] Forgejo container is healthy.
- [ ] Merge Steward container is healthy.
- [ ] Runner stack started only if this release requires it.

## After Deploy

- [ ] `scripts/post-deploy-check.sh` passed and wrote retained private
      `$ELIZA_ARTIFACT_ROOT/eliza-hub-post-deploy-evidence.json`, including
      discovery, OpenAPI, production readiness, production cutover, evidence
      template, workflow, project board, merge queue diagnostics, search, queue
      simulation, work pages, fleet coordination, work context, agent identity
      registry, release readiness, repository protection, agent insights, agent
      capacity, agent performance, agent routing, agent bootstrap, agent
      cockpit, agent action plan, agent submission gate, CI failure analysis,
      PR brief, review assignment, patch conflict prediction, agent inbox API
      smoke checks, and the safe merge queue rollout drill.
- [ ] For production cutover, `scripts/production-evidence-assemble.mjs`
      included `$ELIZA_ARTIFACT_ROOT/eliza-hub-deploy-evidence.json`, and
      `RELEASE_GATE_MODE=production PRODUCTION_EVIDENCE_FILE=... scripts/release-gate.sh`
      passed with private deploy and post-deploy receipt evidence before
      routing real users or enabling live merge execution.
- [ ] `APPLY_BOOTSTRAP=false scripts/bootstrap-forgejo-identity.sh` passed and
      wrote `$ELIZA_ARTIFACT_ROOT/eliza-hub-identity-bootstrap-evidence.json`.
- [ ] `scripts/check-actions-runner.sh` passed if runner is enabled.
- [ ] `scripts/runner-smoke-evidence.mjs` verified `.forgejo/workflows/runner-smoke.yml` if runner is enabled.
- [ ] `scripts/runner-evidence.sh` passed if runner is enabled, and its
      generated `runner` production fragment was copied into private production
      evidence if this is a cutover.
- [ ] Prometheus can scrape `/metrics`.
- [ ] No page-severity alerts are firing.
- [ ] Forgejo invite, password reset, and notification mail still work after
      cutover.
- [ ] Merge Steward worker remains disabled until live merge drill approval.

## Rollback Record

- Previous Forgejo image:
- Previous Merge Steward image:
- Previous runner image:
- Backup bundle:
- Release commit:
- Operator:
- Decision:
